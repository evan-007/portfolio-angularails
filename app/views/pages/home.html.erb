<div ng-app="Portfolio" ng-controller="ProjectsController" ng-init="getProjects()">
	<div class="row">
		<div class="col-md-6">
			<h1>Projects</h1>
			<ul>
				<li ng-repeat="project in projects">
					{{ project.title }} <span ng-repeat="tag in project.tag_list">{{tag}} </span>
				</li>
			</ul>
		</div>
	</div>
</div>