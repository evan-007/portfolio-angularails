<%= tinymce %>
<div class="admin container">
<div class='row'>
	<div class="col-md-8">
		<%= form_for @project do |f| %>
		<p>
			<%= f.label :title %>
			<%= f.text_field :title, class: "form-control" %>
		</p>

		<p>
		  <%= f.label :lede %>
			<%= f.text_field :lede, class: "form-control" %>
		</p>

		<p>
			<%= f.label :description %>
			<%= f.text_area :description, class: "tinymce" %>
		</p>

		<p>
			<%= f.label :git %>
			<%= f.text_field :git, class: "form-control" %>
		</p>

		<p>
			<%= f.label :link %>
			<%= f.text_field :link, class: "form-control" %>
		</p>

		<% if @project.image.exists? %>
		<p>Current image:</p>
		<%= image_tag @project.image.url(:thumb) %>
		<% end %>
		<%= f.file_field :image %><br />

		<p>
			<%= f.label :tags %>
      <%= f.collection_select :tag_ids, Tag.order(:name), :id, :name, {}, { include_blank: true, multiple: true, size: 5} %>
		</p>

		<p>
			<%= f.label "Show on homepage?" %>
			<%= f.check_box :active %>
		</p>

		<%= f.submit %>
		<% end %>
	</div>
</div>
</div>

<script>
    $(document).ready(function() { $('#project_tag_ids').chosen({width: "50%"}); });

</script>
